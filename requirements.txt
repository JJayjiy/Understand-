# --- core app ---
streamlit>=1.40
openai>=1.40
jiwer>=3.0

# --- local / offline transcription (free, no API key) ---
faster-whisper>=1.0

# --- training + evaluating your own fine-tuned model ---
# (install these when you're ready for scripts/finetune_lora.py)
transformers>=4.40
peft>=0.10
accelerate>=0.29
torch>=2.2
soundfile>=0.12
librosa>=0.10
