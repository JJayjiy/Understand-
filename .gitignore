.env
.env.*
!.env.example
.venv/
venv/
__pycache__/
*.py[cod]
data/
recordings/
consent/
corrections.json
sessions.csv
*.wav
*.mp3
*.m4a
*.webm
*.flac
models/
checkpoints/
*.pt
*.bin
*.safetensors
.DS_Store
.ipynb_checkpoints/

# TORGO corpus — NEVER commit. Redistribution violates its terms, and the
# maintainers' approval to publish adapter weights was explicitly conditioned
# on not including any of this. 15GB, ~25k files.
TORGO_datasets/
*.tar.bz2
*.tar.gz
*.zip

# Derived artifacts that can be regenerated from scripts/
eval_results/

# whisperkittools venv — created by scripts/convert_whisperkit.sh, several GB
.venv-wkt/

# Xcode per-user state and build products
ios/**/xcuserdata/
ios/**/project.xcworkspace/xcuserdata/
ios/**/DerivedData/
*.xcuserstate
